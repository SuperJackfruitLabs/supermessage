<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- See values/styles.xml's own note: same style name, dark half. -->
    <style name="Theme.Supermessage" parent="@android:style/Theme.Material.NoActionBar">
        <!-- Dark's `surface`, generated; same reasoning as the light half. -->
        <item name="android:windowBackground">@color/sm_dark_surface</item>
    </style>
</resources>
